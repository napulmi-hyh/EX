"""
투포인트방식
1. return 없음
2. 리스트를 직접 바꿈 (in-place) 
""" 

def reverseString(s:list[str] )-> None:
    left, right = 0, len(s)-1                    #  left(맨앞)/right(맨뒤)
    while left<right:                            # 서로 만나기 전까지 반복
        s[left], s[right] = s[right], s[left]    # 동시교환(swap)
        left+=1
        right-=1
